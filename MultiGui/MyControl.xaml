<UserControl x:Class="MultiGui.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="Control"
             d:DesignHeight="300" d:DesignWidth="300">
  <Border BorderBrush="Red" BorderThickness="5" IsHitTestVisible="False">
    <Viewbox Height="75"  Stretch="Uniform" IsHitTestVisible="False">
      <Ellipse Width="{Binding ElementName=Control, Path=OvalWidth}" 
             Height="{Binding ElementName=Control, Path=OvalHeight}" 
             Fill="{Binding ElementName=Control, Path=OvalBrush}" IsHitTestVisible="False"/>
    </Viewbox>
  </Border>
</UserControl>
